@one to one
private .... vari
(id => construct without id)
vari : setters and getter + constuctor
generate equals
dao -> new interface
extends jpaRespo <class, id_type>

ApplicationCOntect ctx = ....run()

ClassDao bdClass = ctx.getBean(ClassDao.class);


	 *String str = "abc";
String repeated = str.repeat(3);
repeated.equals("abcabcabc");
repeated = new String(new char[n]).replace("\0", s);
String str = "abc";
String repeated = StringUtils.repeat(str, 3);

repeated.equals("abcabcabc");

// create a string made up of n copies of string s
String.join("", Collections.nCopies(n, s));



fichier statci ne besoin pas de controller (under resources/static).
return "filename" (under resources/templates).;
